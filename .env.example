# Bitbucket MCP Server Configuration Template
# Copy this file to .env.test for testing or configure via MCP client

# Bitbucket API Configuration
BITBUCKET_BASE_URL=https://api.bitbucket.org
BITBUCKET_EMAIL=your-atlassian-email@domain.com
BITBUCKET_API_TOKEN=ATATT3x...your-api-token-here
BITBUCKET_WORKSPACE=your-workspace-name

# Test Configuration (for development only)
BITBUCKET_REPO=your-repo-name
TEST_PR_ID=1234

# Instructions:
# 1. Create your API token at: https://id.atlassian.com/manage-profile/security/api-tokens
# 2. Required scopes: Account:Read, Repositories:Read, Pull requests:Read, Issues:Read
# 3. Never commit actual tokens to git - they are in .gitignore for security